/**
 * Shared v4 "Kinetic" tokens. Extracted so the page, nav, agent deck and button
 * primitives all reference one accent + one focus ring without circular imports.
 */
export const ACCENT = "#c6ff3a";

export const focusRing =
  "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#c6ff3a]/70 focus-visible:ring-offset-2 focus-visible:ring-offset-[#0a0b0d]";
